# READ 로그 — 2026-08-24

- 목적: **집계가 아니라 실물을 읽은 기록**. 감사 판정문(`GATE_C_*_AUDIT_*.md`)은
  결론을 담고, 이 로그는 **무엇을 어디서 읽었고 그것이 무엇을 바꿨는지**를 담는다.
- 관계: [[concept-gate-h1-wt/docs/DESIGN_DECISION_mrs_fail_closed_and_rights|D-30]] ·
  `GATE_C_MRS_AUDIT_20260824.md` · `GATE_C_PMB_AUDIT_20260824.md`
- **WSJ 표면은 인용하지 않는다**(Q30.4 + 외부 배포 없음). PMB는 Tatoeba/CC-BY
  이므로 귀속과 함께 인용한다. 표면 공개는 **source별**로 갈린다.
- 읽은 재료는 전부 **로컬 캐시**에 있고 저장소에 들어가지 않는다.

## 1. 조사 회신의 인용 대조 (P12 재실측)

| # | 읽은 것 | 출처 | 결과 |
|---|---|---|---|
| R1 | mirror item 4건 (`20214052`·`21438006`·`21618050`·`20725062`) | 제3자 GitHub mirror, HTTP 200, 3,616/8,985/4,069/4,444 B | 회신 인용 **4/4 일치**. profile·`(active)`·`_most_q<0:4>`·`card("3"/"2"/"5")`·HCONS 전부 확인 |
| R2 | 같은 4건의 **공식** 사본 | Open SDP 1.2 `sdp/2015/eds/*.mrs` | 구조 동일, 표기만 상이(`_rel` 접미사·인용부호·`LTOP`) → **mirror가 날조가 아님을 교차 확인** + adapter 정규화 요구 발견 |
| R3 | `README.open` · `LICENSE.txt` · `2015/eds/README.txt` | 패키지 내부 | 라이선스 3표기 중 하나가 **`CC BY-NC-SA 3.0 Unported`**. 릴리스 이력에 "including raw WSJ and **Brown** strings" — 배포자 자신의 권리 주장 |
| R4 | 공식 SDP 접근 페이지 | `http://sdp.delph-in.net/index.php?page=5` (회신은 timeout으로 BLOCKED) | **HTTP 200**. 회신의 BLOCKED를 권한 차이로 해소 → Open SDP 1.2 경로 확보 |
| R5 | LDC93T1 README | LDC 카탈로그 | `Wall Street Journal Materials, Copyright 1987, 1988, 1989 Dow Jones Inc.` |

## 2. Gate C 사람 감사 — MRS 쪽

| # | 읽은 것 | 결과 |
|---|---|---|
| R6 | `_percent_n_of` 2건 MRS 전문 | **4개 결함 동시 적발**: 표면이 캡션 조각 · 최상위 `unknown_rel`(ERG 분석 실패) · `card(6)`+`_percent_n_of` 동일 label(가산 아님) · `_minimum_a_1`이 관계 수식어인데 시제품이 **`eq`로 오배정**(틀린 oracle) · 두 item이 **바이트 동일** |
| R7 | 생존 7건 표면 | **4건이 `Both`/`All four`** — 집계에서 안 보였다 |
| R8 | 같은 7건의 양화 술어 | `udef_q` 3 · `_both_q` 3 · `_all_q` 1 → E6 신설, 기수 적격 7→**3** |
| R9 | 생존 4건 MRS 전문 | `20413069` 제한식 크기 **2**(형용사+미지어휘) · `20725062` 표면에 **인용부호** · `_u_unknown` 표지 발견(E12) |
| R10 | E0 기각 꼬리 12건 (측정 표지 없는 34건 중) | **12/12 정당 기각**. `two or three`(E13) · `between three and six`(E13) · `Intel 286`(E14) |
| R11 | `at least`/`more than` 표면 5건 | 전부 긴 다중 양화 문장 → **BODY 유일성 0/78**의 원인이 구조적임을 확인 |
| R12 | `_most_q` 근접 6건 표면 | 동반 양화가 **전부 `_the_q`** → **한정사 문제** 발견 (§Q31.1) |

## 3. Gate C 사람 감사 — PMB 쪽

| # | 읽은 것 | 결과 |
|---|---|---|
| R13 | V4 `cardinal` 3 + `proportional` 1의 SBN·raw 전문 | **4/4 기각**. `The most beautiful flowers…`는 `most.r.01`이 **부사**(G1이 잡으려던 그 오탐) · 나머지 3건은 `Quantity -`/`+`(막연 표지, 수치 없음) |
| R14 | PMB gold en 12,053건의 `Quantity` census | `-1`/`+1`/`-2`는 SBN **상대 노드 참조**임을 판별. 진짜 수사는 부호 없는 정수 **106 occurrence** |
| R15 | 그중 표본 4건 전문 | `I'm thirty.`→`measure.n.02 Quantity 30 Unit year` (E0) · `I got slapped on both cheeks.`→`cheek.n.01 Quantity 2` (E6) · `I've attempted suicide twice.`→`time.n.01 Quantity 2` (E7 신설) |
| R16 | 좁은 E0에서 새어 나온 후보 35건 표면 | 전부 단위/부분격/분수/다중문장 → **E8·E9·E10 신설**, E0을 단위 명사로 확장. **집계는 35건을 "후보"라 했고 READ가 0건이라 했다** |
| R17 | 잔존 3건 | `per annum` · `a head taller` · `a fifth of a gallon` → 전부 단위. **PMB 기수 적격 0 확정(전수)** |
| R18 | PMB 15 fixture의 raw | 전부 **단일 문장**, `text_sha256` **15/15 일치** → E10 무영향, V4 commitment 온전 |
| R19 | PMB 15 fixture의 oracle IR(adapter 재생) 제한식 | **이미 2건이 크기 2**(`Is everything you own in that chest?` 관계절 · `Everybody except Joe went to the party.` 예외구) → `20413069` **기각 아님**, 기수 3 유지 |

## 4. 규칙 공허성 검사 (cascade 없이 독립 실행)

| # | 대상 | 결과 |
|---|---|---|
| R20 | E2 독립 | **9,758** — 공허 아님 |
| R21 | E3 독립 | **297** — 공허 아님. 그리고 **정의 오류 교정**: 일괄 배제하면 `_at+least_x_deg`(사상 대상)까지 버린다 → 정본은 "**사상표에 없는** 수식어" |
| R22 | E4 독립 | **428** MRS 바이트 동일 중복 — 독립성 위반 |
| R23 | E15 후보(8,220) 사유 분해 | **전부** `card.ARG1`이 비개체 변수(`i` 8,189 · `e` 31). 개체 변수인데 양화 0/2+는 **0건** |

## 5. 측정기 오류 (이 로그의 존재 이유)

READ 중 **내 측정기가 두 번 틀렸고 두 번 다 잡혔다.**

| # | 오류 | 어떻게 잡혔나 |
|---|---|---|
| M-a | census EP 블록 정규식이 중첩 feature 구조(`ARG0: x5 [ x PERS: 3 ]`)에서 경계를 잘못 잡아 `udef_q`의 RSTR/BODY를 놓쳤다 | 결과("비례 16·기수 2")를 보고하기 **전에** 실물 1건으로 도구를 검증 |
| M-b | PMB 제한식 크기를 `restriction` 필드로 셌더니 **15/15가 0** | desugar가 제한식을 `implies.left`로 옮긴다. **이질적 fixture 15개가 전부 0인 것이 불가능**하다는 판단으로 재측정 |

**규칙**: 이질적 입력에서 균일한 결과가 나오면 0건은 발견이 아니라 **측정기
점검 신호**다. 그리고 규모 주장은 **계약으로 결박된 도구**로만 한다(M18).

## 6. 이 READ가 바꾼 것 — 요약

| 이전 서술 | READ 후 |
|---|---|
| "기수 재료 0/3·비례 0/1" (V4 배정 근거 불명) | V4 배정 4건은 **실물로 기각 확정**(최상급 1 + 막연 표지 3) |
| "PMB가 기수를 공급할지 미확인" | **전수로 0 확정** — 수사 106건 전부 배제 사유 |
| "기수 적격 11건" | 사람 감사로 **3건**(하한과 동일, 여유 0) |
| "`rel` 재료 부재" | `at least` 156건 존재하나 **BODY 유일성 0/78** — 전수 확정 |
| "여유 0이 치명적" | **한정사 한 문항**이 비례 1→7·기수 11→104. 후보 풀은 11이 아니라 **100 규모** |
| 배제 규칙 7개 중 4개가 0건 | 독립 실행으로 **3개 실체 확인**, E3 정의 오류 교정, 미검사는 E5뿐 |
