{
  "_comment_macos": "~/Library/Application Support/Claude/claude_desktop_config.json",
  "_comment_windows": "%APPDATA%\\Claude\\claude_desktop_config.json",
  "_comment_install": "repo 루트에서 `python3 -m venv .venv && .venv/bin/pip install -e .` 후 아래 경로 사용",
  "mcpServers": {
    "conceptgate": {
      "command": "/absolute/path/to/concept-gate-taxonomy/.venv/bin/conceptgate-mcp",
      "args": []
    }
  }
}
