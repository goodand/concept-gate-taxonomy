# E2.4 이슈 등록부

- 갱신: **2026-07-29** — cohort 실행 완료 + 외부 운영 변경 지시 대응 +
  **D4(제약 #11 리뷰) 실행 완료로 인증 3/3 복구**, D5(재실행 규모) 정책 판정,
  H1a 외부 설계 판정 반영
- 문서 종류: **운영 로그**(`WORKSPACE_NAVIGATION.md` §2) — 계속 갱신, 동결
  아티팩트와 같은 커밋에 섞지 않는다
- 역할 분담: `HANDOFF.md`가 **요약과 다음 행동**, 이 문서가 **전체 목록과 근거**.
  둘이 어긋나면 이 문서가 상세 쪽이고 HANDOFF가 진입점이다
- 표기: **[GATE]** 이것만 풀리면 진행 / **[DESIGN]** 설계 판단 필요 /
  **[OPEN]** 미결 / **[EXT]** 별도 실험 / **[DONE]** 해결(재발 감시용 보존)

---

## 0. 상태 요약

| 항목 | 값 |
|---|---|
| 브랜치 / HEAD | `codex/e2.4-contract-repo-design` (푸시됨) |
| 워킹트리 | clean |
| 게이트 | E2.4 **78 passed**, 형제 실험 6·5, 코어 red 1건(기존 owlready2) |
| 인증된 class | **3/3, 제약 #11 리뷰 완료** — 30/30 `ok`, `unknown` 0 ([DONE] #25) |
| clean rerun cohort | **실행 완료** — `cohort_score.json` 참조. `protocol_deviation` 없음(N=10 정합) |
| 3-arm 본 실험(H3) | **미실행**, 선행 설계 과제 D3(H1c)·`_gen_prompts.py` 부재 미해결 |
| **진행을 막는 것** | **D3(H1c) 커버리지 재설계** — H3 스코핑의 선행 조건. D4·D5는 해결됨 |

> **2026-07-29 외부 운영 변경 지시 수신.** 지시문은 E2.4 실행·평가 지시를
> 대체하며 핵심은 *"모델 입력 경계가 구현과 테스트로 고정될 때까지 실행을
> 멈춘다"*이다. 지시문의 **8개 차단 조건은 전부 이미 충족**됐음을 실측으로
> 확인했다(아래 표). 다만 **정말로 어긋난 것 3건**이 있고, 그중 D4가 중대하다.
>
> 지시문은 이 세션의 진행보다 앞서 작성돼 "현재 인증된 class 수는 0"을
> 전제하지만, 실측은 3/3이다. 이 차이는 롤백 사유가 아니라 **보고 사항**이다.

### 지시문 8개 차단 조건 — 실측 대조 (2026-07-29)

| # | 조건 | 증거 |
|---|---|---|
| 1 | 제작용 fixture와 모델-facing payload 분리 | `test_builder_only_fields_never_appear_in_payload` |
| 2 | 정본 `build_model_payload()` 화이트리스트 builder | `_surface.py:314`, `MODEL_PAYLOAD_KEYS`/`MODEL_EVIDENCE_KEYS` 선언 후 필드별 구성 |
| 3 | 실행 전 qualification gate | `_surface.py:319` — `status != 'passed'`면 `SurfaceError`로 payload 생성 거부. manifest를 인자로 요구해 "검증 건너뜀" 상태가 존재 불가 |
| 4 | 모델 입력 필드 닫힌 집합 | 실측: top-level 3개, evidence item 정확히 `evidence_id`/`source_kind`/`text` |
| 5 | rendered prompt·스키마 hash 기록 | 지시문 요구 9항목 **전부** + `system_prompt_sha256`·`presented_schema_sha256` 2개 추가 |
| 6 | hidden-field noninterference + 유출문 positive-control | `test_builder_metadata_cannot_move_the_payload_one_byte`, `test_known_leak_sentence_never_reaches_the_rendered_prompt[0..5]` |
| 7 | 루트에서 실제 수집·통과 | `scripts/run_gates.py` → `[ok] e2.4 … 78 passed` (지시 수신 시점엔 35) |
| 8 | 모든 실행 경로가 동일 canonical builder | `test_canonical_pipeline_pins_the_whole_surface` |

**금지 필드 14종 전수 부재 실측 확인** — `source_path`, `locator`, `source_ref`,
`text_sha256`, `builder_metadata`, `qualification*`, `liveness`, `authority`,
`supersession`, `semantic_class`, `expected_decision`,
`expected_contract_verdict`, `fixture_version`, `source_commit` 전부 payload
canonical JSON에 부재.

### ACK 회신 (지시문 §5 형식) — **2026-07-29 발신 시점의 원문. 이후 D4·D5가 해결됐으므로 `conflicts`·`next safe action` 항목은 현재 상태가 아니다**

```text
ACK
session/agent:        E2.4 실험 운영 세션 (worktree concept-gate-e2.2-wt,
                      branch codex/e2.4-contract-repo-design)
current task:         지시문 대조 완료. 신규 trial 미실행, 차단 유지 중
execution status:     completed — clean rerun cohort 30 trial (2026-07-28)
                      지시 수신 시점에 running 상태인 trial 없음
affected artifacts:   _surface.py, _cohort.py, _score.py, contract_prompt.md,
                      decision_schema.json, fixture_*.json(4), oracle_manifest.json,
                      cohort_prompts.json, trials_raw.json, trials.json,
                      cohort_score.json, e2.4-contract-decider.md,
                      test_surface.py, test_protocol.py
legacy_leaky:         legacy_leaky.md — 4건, 항목별 제외 사유 기록 완료.
                      삭제하지 않음(지시문 §1 준수). 인증·통계에서 제외됨
stopped actions:      없음 — 지시 수신 시점에 실행 중 trial 없음.
                      신규 trial은 D4·D5 판정 전까지 실행 차단
uncommitted changes:  없음 (커밋·푸시 완료)
conflicts:            C1 명칭 불일치 → 정합 완료(DONE #24)
                      D4 제약 #11이 UNKNOWN인데 인증 발생 → 판정 요청 (중대)
                      D5 재실행 규모 17(지시문) vs 30(사전등록 N=10/cell) → 판정 요청
next safe action:     D4 (a) 승인 시 _review_11.py 신설 — 기존 30 trial의
                      rationale 재검토. 새 trial 불필요.
                      D5 판정 대기. 그 전까지 신규 trial 없음
```

**지시문 §5의 "이미 완료했다면 사용한 실제 모델 입력 표면과 payload 생성 경로를
보고하세요"에 대한 답**:

- payload 생성 경로: `fixture_*.json` → `_surface.qualify_fixture()`(실행 전
  qualification, 실패 시 거부) → `_surface.build_model_payload(fixture, manifest)`
  (화이트리스트, manifest 필수 인자) → `_surface.render_prompt(contract, payload)`
  → `_cohort.py freeze` 가 바이트와 해시 고정 → Workflow가 그 바이트를 그대로 전달
- 모델 입력 표면: user prompt(`rendered_prompts[fixture_id]`, fixture당 해시 1개) +
  system prompt(`e2.4-contract-decider.md`, `tools: []`, 출력 계약 포함).
  두 채널 모두 해시 기록됨(`rendered_prompt_sha256`, `system_prompt_sha256`)
- 손으로 구성한 payload는 사용하지 않았다 — 모든 경로가 동일 canonical builder를
  거침(조건 8, `test_canonical_pipeline_pins_the_whole_surface`)

**G1 해소 상세**: 30 trial 실행 중 22개가 세션 사용 한도("session limit, resets
11:40pm KST")로 실패 — API 세션 한도는 컨텍스트 윈도우 토큰과 별개 지표이며,
전송 실패이지 trial 데이터가 아니므로 기록하지 않았다. 한도 리셋 시각을
확인한 뒤 실패한 22개만 별도 batch로 재실행해 22/22 성공. 두 batch를 병합해
30/30 확보 후 `record`→`_score.py`. 결과는 검증 문자열 일치뿐 아니라
**실질 검증**까지 통과했다 — E24-F-02는 10/10 전부가 필러 feature `갑종`을
`insufficient`로 정직하게 표시하며 `바퀴`만 repair(계약 규칙 5가 요구하는
정확한 패턴), E24-F-03은 10/10 전부가 evidence를 `indirect_context`로 정확히
분류(규칙 2의 온톨로지적 성격 서술 요구를 충족 못하는 코드 서술이므로).

---

## [DESIGN] 설계 판단 대기

### D1. 결정문에 없던 강제 귀결 2건 — 이미 구현, 승인 확인 필요

**D1a. `evidence_audit.source_path` → `source_kind`.**
§2의 3면 분리로 모델 payload에서 path가 사라졌는데 스키마는 여전히 path를
요구하고 있었다. 그대로 두면 **스키마가 환각을 강제**한다. 선택이 아니라
강제였으나 `DESIGN_DECISION_surface_separation.md`에 없는 변경이다.

**D1b. §6 해시 목록의 구멍.** `rendered_prompt_sha256`이 "모델이 본 전체
표면"을 고정한다고 §6이 명시하지만 **output schema와 system prompt를 덮지
못한다.** 전송 제약으로 출력 계약을 system prompt로 옮기면서 드러났다.
`system_prompt_sha256`·`presented_schema_sha256`을 추가했다 — 사실상 §6 개정.

### D2. H1b — 규칙 3의 `conflicting` 정의: 구현했으나 **검증 불가**

5단계 절차로 규칙 3 본문에 명시하고, `conflict`를 admissibility enum에서
제거하고 `conflicts_with_evidence_ids` 대칭성을 강제했다.

**그러나 E2.4 안에서는 검증할 수 없다** — 검증에 쓸 `conflicting` fixture가
바로 미확보로 종결된 그 fixture다. "텍스트상 해결, 경험적 미검증" 상태.

부수 효과는 확인됨: `PROBLEM_2_conflicting.md` §5.1 trial 4의 내적 모순
(ev5/ev6를 둘 다 `conflict`로 두면서 "neither provides direct_support")은
**새 스키마에서 표현 자체가 불가능**하다.

### D4. 【해결 — 2026-07-29】 제약 #11이 `UNKNOWN`인데 인증이 났다 → 리뷰 완료, 인증 복구

> **사용자 판정: (a) 채택 → 실행 완료.** 기존 30 trial의 rationale을 독립
> 리뷰어가 전수 재검토했다. **새 trial 없음.** 결과: **30/30 `ok`, 위반 0건,
> `unknown` 0건 → `certified 3/3` 복구.** 상세는 [DONE] #25.
> 사전등록 문서는
> [`DESIGN_D4_constraint_11_review.md`](../experiments/2026-07-25_e2.4_repo_grounded_contract_transfer/DESIGN_D4_constraint_11_review.md).
>
> **부수 판정 (D5 연동)**: 재실행 규모 논쟁은 "17 vs 30 택일"이 아니라
> **단계적 조기중단 정책**으로 해소한다 — smoke 규모를 먼저 돌려 임계점 미달
> 조건을 확인하고, 미달이면 실험을 수정, 미충족이면 이어서 진행한다.
> 리뷰 단계에 적용한 형태가 위 설계문서 §6이다.

#### D4 (a) 구현 현황 (2026-07-29, 미커밋)

| 산출물 | 내용 |
|---|---|
| `DESIGN_D4_constraint_11_review.md` | **사전등록.** 리뷰어 표면 분리, 판정 어휘, 조기중단 규칙, 교정 요구를 리뷰 실행 **전에** 고정 |
| `_review_11.py` | 결정론 계층 — 화이트리스트 payload 빌더, 인용 검증, UNKNOWN 규칙, 조기중단 산술, 교정 게이트 |
| `test_review_11.py` | **43 tests.** 축 전수(0/1/2+), 뮤테이션 3종으로 recall 확인 |
| `review_11_calibration.json` | 라벨 코퍼스 15건(recall 5 / precision 7 / cardinality 3). **실행 전 커밋** |
| `e2.4-review-11.md` | 리뷰어 subject(`tools: []`), `REVIEW_SCHEMA`에서 생성 |
| `_score.py` | `clean`을 **4중 논리곱**으로 확장(verdict + 스키마 + 준수 + #11 리뷰) |

**설계상 중요한 결정 3가지**:

1. **리뷰어에게도 표면 분리를 적용한다.** 리뷰어도 모델이므로 오라클을 주지
   않는다. 다만 실측 결과 **verdict 어휘가 trial 자신의 rationale에 들어
   있다**(30 trial 중 `accept_report` 23건). 빌더가 넣은 게 아니라 모델이 스스로
   쓴 것이라 제거 불가 — 따라서 "리뷰어는 결론을 모른다"고 **주장하지 않는다**.
   상세는 설계문서 §3.1.
2. **`unknown`은 하네스가 부여하고 리뷰어는 자칭할 수 없다.** 리뷰어 enum은
   `ok`/`violation` 2개뿐이다. "unknown" 선택지를 준 검증기는 그것을 도피처로 쓴다.
3. **인용 없는 `violation`은 무효.** 인용문이 원문에 바이트 그대로 없으면
   `unknown`으로 강등한다. 리뷰어 환각이 인증을 깎는 것을 막는다.

**중간 상태(리뷰 실행 전, 이력용)**: 채점기가 `certified 0/3`, 각 cell
`#11 review: {'unknown': 10}`, `pre_review_clean_hits` 10/10을 보고했다.
회귀가 아니라 D4가 지적한 상태를 처음으로 정직하게 표시한 것이었고, 지시문
§3대로 검증 결과 없음이 통과가 아니라 차단으로 작동함을 보였다.

**최종 상태(리뷰 완료)**:

```
E24-F-01  clean 10/10 (1.0, screened_PASS)   #11 review: {'ok': 10}
E24-F-02  clean 10/10 (1.0, screened_PASS)   #11 review: {'ok': 10}
E24-F-03  clean 10/10 (1.0, screened_PASS)   #11 review: {'ok': 10}
certified 3/3 classes    certification_blocked 필드 소멸
```

**agent registry 제약은 예상보다 일찍 해소됐다** — [DONE] #17을 근거로 다음
세션을 예상했으나, 정의를 설치한 뒤 같은 세션에서 `e2.4-review-11`이 레지스트리에
올라왔다. #17의 "세션 시작 시점 고정"은 이번 관측과 어긋나므로, 그 항목은
**항상 참인 제약이 아니라 당시 관측**으로 읽어야 한다.

### D4 원래 문제 서술 (이력 보존)

지시문 §3: *"검증기를 실행할 수 없거나 검증 결과가 없으면 `FAIL`로 추정하지
말고 `UNKNOWN`으로 기록하고 **실행을 차단**합니다."*

`semantic_constraints` #11("모델은 출처의 liveness나 우선순위를 재판정하지
않는다")은 자연어 rationale을 읽어야 판정되므로 기계 검사가 불가능하고,
그 리뷰가 **채점 흐름에 편입돼 있지 않다**(기존 O1).

즉 **3/3 인증은 #11에 대한 검증 결과가 없는 상태에서 나왔다.** 지시문 §3을
적용하면 그 항목은 `UNKNOWN`이었어야 하고 실행이 차단됐어야 한다. 이것이
지시문이 실제로 잡아낸 유일한 실질 결함이다.

선택지(설계 담당 판정 필요):

| 안 | 내용 | 비용 |
|---|---|---|
| **(a) 권장** | 기존 30 trial의 rationale을 독립 리뷰어가 읽어 #11 판정 → `clean` 정의의 4번째 항으로 편입 | **새 trial 불필요.** 기존 결과 재검토만 |
| (b) | #11 미검증을 명시한 채 인증을 `3/3 (with UNKNOWN #11)`로 강등 표기 | 없음, 단 인증의 의미가 약해짐 |
| (c) | 인증을 0으로 되돌리고 (a) 완료 후 재인증 | (a)와 같으나 중간 기간 인증 공백 |

(a)를 권장한다 — 비용이 가장 작고 지시문의 UNKNOWN 규칙을 실제로 충족시킨다.

### D5. 【해결 — 2026-07-29】 재실행 규모 — 지시문의 "17 trials" vs 사전등록 N=10/cell

> **사용자 판정: 단계적 조기중단 정책.** 17/30 택일이 아니라, smoke 규모를 먼저
> 돌려 **임계점 미달 조건을 확인**하고 미달이면 실험을 수정, 미충족이면 이어서
> 진행한다. 기존 30 trial은 그대로 유지하며 재실행하지 않는다.
> 이 정책을 #11 리뷰에 적용한 형태가
> `DESIGN_D4_constraint_11_review.md` §6이다(미달 조건 = cell당 위반 2건).
> 아래는 판정 이전의 원 서술이다.


지시문 §4는 "기존 **17 trials**를 전부 새 cohort로 실행"이라 쓴다. 실제 실행은
**30 trial(10/10/10)** 이다. 근거와 경위:

- 사전 등록 Stage 1 프로토콜이 **N=10/cell**이고 판정 밴드가 그 N에 보정돼 있다
- 17(7/5/5)은 폐기되는 legacy trial 수를 반영한 숫자였고, 그 N에서는 N=5의
  ambiguous 밴드가 trial 1개 폭, N=7의 6/7=0.857이 프로토콜 표에 이름 없는
  구간에 떨어진다
- **아무 trial도 돌기 전에** 재동결했으므로 freeze-before-run 위반이 아니다
- 재동결 후 `rendered_prompt_sha256`은 fixture별로 이전과 **바이트 동일**
- 결과에 `protocol_deviation` 없음

→ **지시문의 문자(17)와 사전등록 프로토콜(10/cell)이 서로 충돌한다.** 어느 쪽이
우선인지 판정 필요. 30 유지가 프로토콜 정합이라고 보나, 17을 고수하면 재동결·
재실행이 필요하고 그 경우 현 30 trial 결과는 `non_certifying_partial`로
강등해야 한다. 지시문 §5("설계와 충돌하는 구현을 발견해도 임의로 확장 수정하지
말고 충돌 사항으로 보고")에 따라 **임의 결정하지 않고 올린다.**

### D3. H1c — Phase 5 커버리지 재설계 (H3 선행, 미해결)

기존 설계는 CONTROL_REPO/A_REPO에 `sufficient_consistent` + `conflicting`
2개를 배정했다. `conflicting`이 빠지면서:

- **arm 비교의 최고 신호 셀이 사라졌다.** 이 실험 전체를 동기부여한 유일한
  arm 비교 관측(legacy 2개 arm은 조용히 repair, CONTRACT_REPO만 abstain)이
  바로 그 fixture에서 나왔다
- **그 관측 자체가 오라클 유출 packet 기반 N=1이라 재현이 필요하다**
- abstain-target class 중 남는 건 `insufficient` 하나뿐
- 대안(미결정): `conflicting` 자리에 `insufficient`를 넣을지,
  `sufficient_repairable`을 넣을지, `insufficient` 단독에 N을 늘릴지

---

## [OPEN] 미결

### O1. (→ **[DESIGN] D4로 승격**) `semantic_constraints` #11이 기계 검사 불가

2026-07-29 지시문 §3의 `UNKNOWN` 규칙이 이 항목을 **단순 미결이 아니라 인증
유효성 문제**로 만들었으므로 [DESIGN] D4로 옮겼다. 아래는 기술적 사실만 유지.

제약 11개 중:

| 상태 | 개수 | 항목 |
|---|---|---|
| 검사됨 | 10 | (#4·#6은 `e03f74a`에서 추가) |
| **기계 검사 불가, 담당 미지정** | 1 | **#11 — "모델은 출처의 liveness나 우선순위를 재판정하지 않는다"** |

#11은 자연어 rationale을 읽어야 판정되므로 어떤 검사기도 커버할 수 없다.
필요한 것: 독립 리뷰어가 각 trial의 rationale을 읽고 "출처가 더 최신이다/아직
살아 있다/더 권위 있다"를 근거로 충돌을 해결했는지 판정하는 단계를
`_score.py` 이후에 배치. 판정 결과를 `clean` 정의의 네 번째 항으로 넣는다.
리뷰 불가·결과 없음이면 지시문 §3대로 `UNKNOWN`으로 기록하고 인증을 보류한다.

### O2. H3 본 3-arm 실험 미실행 — 선행 엔지니어링 부재

- 규모: 8 cell × N=10 = **80 trial**(CONTRACT_REPO 4 class + legacy 2 arm ×
  2 class). 단 D3 미해결로 이 배분 자체가 재설계 대상
- **Phase 4의 `_gen_prompts.py`가 존재하지 않는다**(확인함). CONTROL_REPO/
  A_REPO용 legacy 프롬프트 템플릿을 새로 작성해야 한다 — 별도 스코핑 필요
- 유일한 arm 비교 실측이 N=1이고 그마저 유출 packet 기반

### O3. (해소, [DONE] #21 참조) legacy 결과 4건 제외 — 새 인증으로 대체됨

`legacy_leaky.md`에 항목별 사유 기록. 요약:

| fixture | 기존 실측(제외됨) | 제외 사유 | **새 인증(2026-07-28)** |
|---|---|---|---|
| E24-F-01 | 7/7 accept_report | v1 `extraction_note` 유출 | **10/10 clean, screened_PASS** |
| E24-F-02 | 5/5 repair | 유출("supports structural_composition, not essential_feature") | **10/10 clean, screened_PASS** |
| E24-F-03 | 5/5 abstain | 유출("should classify ... never direct_support") | **10/10 clean, screened_PASS** |
| E24-F-04 | decision 5/5 abstain, verdict 4:1 | **유출은 아님** — 정본 빌더 미경유, `rendered_prompt_sha256` 없음, 이후 계약 문구 변경 | 코호트 제외 유지(미확보 종결) |

용어 규율: 새 실행은 "재채점"도 "재현"도 아닌 **clean rerun cohort**였다.
legacy 숫자와 새 숫자는 **우연히 비슷하지만(전부 만장일치) 같은 것을 잰 게
아니다** — legacy는 유출 payload, 새 코호트는 화이트리스트 빌더를 거친
payload다. 이 표는 비교를 위한 것이 아니라 어떤 숫자가 폐기됐고 어떤 숫자가
대체했는지 기록하기 위한 것이다.

### O4. 저장소 코드 3건 — 승인 대기 (E2.4가 read-only로 취급)

- **O4a** `test_cg_obligations.py::test_registered_handlers_resolve`가
  `owlready2` 부재로 **스킵이 아니라 FAIL**. 저장소는 이미 3곳에서
  `pytest.importorskip("owlready2", ...)` 관례를 쓰는데 이 테스트만 예외.
  **기존 결함**(변경 전에도 동일, `git stash`로 확인). 제안 패치는
  `HANDOFF.md` §10.1 — 파일 상단 `importorskip`은 **안 됨**(같은 파일 24개가
  통과 중)
- **O4b** `has_part`/`part_of`가 `RELATION_HINT_TYPE`에 없어
  `relation_discrimination_gate`가 `essential_feature`+`has_part`를 is-a DAG에
  통과시킨다. `docs/MCP_SERVER.md`와 `server.py` 가이드는 반대로 안내 —
  **문서와 코드가 상충하는 라이브 버그**
- **O4c** `cg_partwhole.py:7-8` stale docstring("참조용 — 직접 import하지
  않음"). 이 문장이 잘못된 "죽은 코드" 판정을 만들었고 lesson은 정정됐으나
  **코드 주석은 미수정**

---

## [EXT] 별도 실험으로 분리

- **H1a `source_authority_unresolved`** — **설계 판정 완료(2026-07-29), 사전등록
  미완.** 폴더: `experiments/2026-07-29_h1a_source_authority_unresolved/`.
  재료 확보·검증됨: `docs/phase_a_implementation_packet.md:102`(철=칼의 재료→
  `essential_feature`, superseded) 대 R6b/`cg_partwhole.py`(`material_of`→
  `structural_composition`, 통과 중). **인스턴스까지 `칼`/`철`로 정확히 일치**.

  경위: 초안 → 4축 적대 검증(blocker 2건 lead 재실측으로 기각, 초안의 "유출
  딜레마" 전제 반증) → 외부 설계 판정 7건 수령(OpenAI Codex, 저장소 접근 없는
  자족형 프롬프트로 요청). 판정 결론은 **"재정의 필요"**:

  | 항목 | 판정 |
  |---|---|
  | 채점 극성 | **정답 없음.** 서술적 행동 측정. E2.4 인증 체계·밴드 미사용 |
  | arm 개수 | **2-arm** (`PROHIBITION_KEPT` / `PROHIBITION_REMOVED`). legacy CONTROL은 "보류"를 표현 못 해 종속변수를 구조적으로 검열하므로 제외 |
  | 구현 위치 | **H1a 전용 사본.** E2.4 동결 아티팩트 불변 |
  | 판정 어휘 | **H1a 전용 스키마.** hidden oracle 없음. 중립 어휘(`select_type`/`defer`) + 후처리 분류(`selection`/`deferral`/`invalid`) |
  | arm별 채점 | **양쪽 모두 correctness 채점 안 함.** #11도 주평가 미적용 |
  | 최소편집 | 지정된 **한 문장만** 삭제, byte-level 테스트 + 해시 동결 |
  | 인과 귀속 | **하지 않음.** "행동이 관측됐다"까지만 |

  > **해석 경계(판정문 명시)**: 모델은 실제 liveness 정보를 받지 않는다.
  > 따라서 측정 대상은 "live 코드를 알아보는가"가 **아니라** "출처 종류가
  > 충돌할 때 금지 문장 유무가 선택/보류 행동에 만드는 차이"다. 결과 서술에서
  > 이름이 함의하는 것 이상을 주장하지 않는다.

  **실행 차단 중** — 판정문이 사전등록 7항목(시행 수·randomization·parameters·
  제외 기준·**행동 코딩 규칙**·**invalid 처리 규칙**·종료 기준)을 실행 조건으로
  달았다. 특히 행동 코더는 정답 없는 실험의 **유일한 판정 장치**이므로
  결과 확인 **전에** 동결하고 recall/precision 양방향 테스트를 붙여야 한다
  ([DONE] #13·#16과 같은 자리).

  원문·상세: `DESIGN_DECISION.md`, `README.md`, 요청서 `DESIGN_REQUEST.md`(내부용)
  / `DESIGN_REQUEST_PROMPT.md`(외부 자족형), 검증 보고
  `docs/feedback/h1a_source_authority_unresolved_review_20260729.md`
- **H2 cross-concept invariant fixture** — 4/4 abstain의 원인이 "evidence
  부재"인지 "cross-concept 전이 자체 거부"인지 분리. 후자면 CONTRACT_REPO가
  전역 invariant를 사실상 집행하지 못한다는 뜻이라 E2.3 결과의 전이 가능성에
  중대한 제약. 후보 `돌체`/`돌체린`은 서로 다른 type을 가리켜 설계 주의 필요
- **H4 whole-packet vs scoped 판정의 취약성 비대칭** — 동일 필러가
  `accept_report`는 5/5 차단, `repair`는 5/5 통과. 우선순위 낮음

---

## [DONE] 해결 이력 — 재발 감시용

**검증 강도** 열이 중요하다. "게이트 고정"만이 재발을 막는다.

| # | 결함 | 해결법 | 검증 강도 |
|---|---|---|---|
| 1 | **오라클 유출, 4/4 fixture 전부.** v1 `extraction_note`가 모델 payload와 같은 dict에 있었고 기대 판정이 평문으로 들어 있었다. 스키마 `description`이 금지했으나 **아무것도 그 문장을 실행하지 않았다** | 3면 분리 + 화이트리스트 빌더. 유출 문장 6개를 `KNOWN_LEAKS` positive control로 커밋 | **게이트** (`test_surface.py` 12·13·14·15~20) |
| 2 | **"나머지 3개는 clean"이라는 거짓 보고** | grep 재조사 → 4/4 확인, 인증 3→0 철회(`4a14fdd`) | **게이트** (동일 코퍼스) |
| 3 | **금지어 가드를 "기계적"이라 과장** — 실제 유출 4건에 **전부 회피** | 진단 전용으로 강등, 집행을 빌더 구성으로 이전. 가드를 원시 fixture가 아니라 빌드된 payload에 조준 | 실측 1회 + 게이트(테스트 5) |
| 4 | **ev6 provenance 거짓** — "one commit later" → 실제 3개, 중간 `ce3699a`가 앞 커밋 주장이 이미 실패로 측정됐음을 기록 | `git log` 재확인 후 정정, **정정 사실 자체를 노트에 보존** | 실측 1회 |
| 5 | **"죽은 코드" 전제 오류** — docstring이 stale | import 확인 | 실측 1회. ⚠️ 코드 주석 미수정(O4c) |
| 6 | **`experiments/` 모듈명 충돌** — `_cert_core.py` 6개·`evaluate.py` 10개·`_gen_prompts.py` 7개 동명 → 07-23 실험이 07-19 evaluator로 실행돼 `KeyError: 'role'`. **중복은 동결 규율의 산물이라 제거 불가** | `scripts/run_gates.py` 프로세스 격리 | **게이트** (러너 출력 6·5·33) |
| 7 | 루트 수집 0개 | `--import-mode=importlib` | 실측(89개 수집) |
| 8 | **러너 BLOCKED 로직 버그(자체 발견)** — owlready2 1건으로 코어 77개를 가릴 뻔 | pytest `rc==2` 의미론 | **게이트** |
| 9 | 러너가 `test_protocol.py`만 glob → `test_surface.py` 누락 | 실험 디렉터리 전체 수집 | **게이트** (테스트 33) |
| 10 | 필러 feature가 `accept_report` 5/5 차단 | 제거 | ⚠️ 근거 수치가 `legacy_leaky` |
| 11 | cross-concept 비전이 4/4 거부 | 목표를 single-concept으로 교체 | ⚠️ 근거 수치가 `legacy_leaky`, 발견은 H2로 보존 |
| 12 | **스키마 이중화(이번 세션에 자체 생성)** — 전송 제약으로 출력 계약이 두 곳에 | agent 정의를 생성물화, `freeze()`가 stale이면 거부, 테스트로 고정 | **게이트** (테스트 6) |
| 13 | **채점기가 계약이 명령한 행동을 위반으로 집계** — `conformance()`가 5단계를 packet 전역으로 1회 도출해 모든 per-feature 판정과 대조. 규칙 5가 지시한 필러 처리가 위반으로 잡혀 **E24-F-02 `clean_rate`=0 확정이었다** | `feature_judgments[].evidence_ids`로 부분집합을 뽑아 **feature별** 도출 | **게이트** (테스트 10, 실제 fixture+payload) |
| 14 | `_score.py`가 `record()`의 `schema_violations`를 안 읽음 — 구조적으로 무효한 출력이 인증에 집계 가능 | clean 정의를 3중화(verdict + 스키마 + 준수) | 코드만 (실데이터 부재) |
| 15 | `semantic_constraints` #4·#6 미검사 | `conformance(out, payload)`로 payload를 **필수 인자**화 | **게이트** (테스트 9) |
| 16 | 검사기 테스트 케이스가 전부 `feature_judgments` 1개 — 버그가 그 축에 있었다 | 다중 feature 케이스 추가 | **게이트** (테스트 10) |
| 17 | **agent registry가 세션 시작 시점에 고정** — 세션 중 만든 trial subject를 못 찾음 | 정의를 커밋·설치하고 다음 세션에 위임. **`e2.2-decider`로 대체하지 않았다**(그 system prompt가 이 payload에 없는 `input_concepts`를 지목 → 인증 실행 오염) | 실측(다음 세션에서 등록 확인) |
| 18 | **structured-output 스키마 크기 거부** ("output schema too large to classify safely", 설명 제거 4.7KB에서도 동일) | 출력 계약을 trial subject의 system prompt로 이동. 동결 프롬프트가 필드를 나열하지 않으므로 `rendered_prompt_sha256` 불변 | **게이트** (테스트 7) |
| 19 | 안전 분류기가 크기 이분 탐색을 "우회 시도"로 차단 | 탐침 중단, 설계 변경으로 전환. **한계값은 미상으로 남긴다** | — |
| 20 | rogue subagent 무단 commit/push 2회 | 사용자 사후 승인 확인 | — |
| 21 | **G1 해소 — cohort N=10/cell 재동결·실행, 3/3 class 인증.** 실행 중 22/30 trial이 "session limit, resets 11:40pm KST"로 실패 — 컨텍스트 윈도우 토큰과 별개인 API 세션 한도. 전송 실패는 데이터로 기록하지 않고, 리셋 시각 확인 후 실패분만 별도 batch로 재실행 | 리셋 후 batch2 22/22 성공, 두 batch 병합 → 30/30. `record`/`_score.py` 통과, `protocol_deviation` 없음(N=10 정합) | **게이트** (`cohort_score.json`: clean 10/10 ×3, `conformance_violations`=0, `schema_violations`=0) + **실질 검증**(E24-F-02 필러 처리·E24-F-03 indirect_context 분류를 30건 전수 확인) + **적대적 재검증**(#22) |
| 22 | **인증 결과(#21) 자체의 신뢰도 미상.** "10/10 만장일치"는 legacy 유출 실행도 냈던 패턴(7/7·5/5·5/5)이라, verdict 문자열 일치만으로는 유출·과최적화·채점기 재발 결함을 배제 못한다 | **적대적 재검증**: 독립 haiku-model subagent에게 11개 원자 체크(trial count, verdict 재계산, `_score.py` 재실행, E24-F-02 갑종/바퀴 전수, repair 완전성, E24-F-03 admissibility 전수, 프롬프트 결정론, tool_use 격리, 자기인용, report 다양성) 위임, 전부 CONFIRMED. **그 보고 자체를 다시 의심**해 6개 항목을 별도 스크립트로 직접 재현(`_score.py` 바이트 diff, 갑종/바퀴 10건, indirect_context 10건, 프롬프트 해시, 자기인용 3-fixture, tool_use는 raw `agent-*.jsonl`에서 하이쿠와 다른 방법으로 재계산 — 52개 agent 전부 `agentType=e2.4-contract-decider`, `tool_use` 블록 0개) — 전부 일치 | **적대적 게이트 통과** — recall/precision 양방향은 아니지만(negative case 없음), 서로 다른 방법으로 재현되는 recall 검증. 3/3 인증의 신뢰도를 "operator 자기보고"에서 "독립 재현"으로 격상 |
| 23 | **2026-07-29 운영 변경 지시 수신** — 지시문이 8개 차단 조건 미구현을 전제로 신규 trial 실행 금지를 명령. 실제로는 전부 구현·통과 상태였고, 지시문은 이 세션 진행보다 앞서 작성돼 "인증 0"을 전제 | 8개 조건을 실측 대조해 전부 충족 확인(§0 표), 금지 필드 14종 payload 부재 전수 확인. 어긋난 3건만 정직하게 올림 — C1(명칭)은 즉시 정합, D4(#11 UNKNOWN)·D5(17 vs 30)는 임의 결정하지 않고 설계 판정으로 올림(지시문 §5 준수) | **게이트** — 실측 스크립트 + `run_gates.py` 35 passed |
| 25 | **제약 #11이 `UNKNOWN`인 채 인증이 났다(D4).** 11개 제약 중 #11만 자연어 rationale을 읽어야 판정되는데 그 리뷰가 채점 흐름에 없었다. 지시문 §3은 검증 결과 없음을 차단 사유로 규정한다 | **독립 리뷰어(`e2.4-review-11`, `tools: []`)로 30 trial 전수 재검토.** 새 trial 없음. `clean`을 **4중 논리곱**(verdict + 스키마 + 준수 + #11)으로 확장. **결과 30/30 `ok`, 위반 0, `unknown` 0 → `certified 3/3` 복구** | **게이트 + 교정 + 독립 재현** — 상세는 아래 |
| 24 | **`conflicting_evidence` 표시 명칭이 지시문과 불일치** — 로컬 조어 `no_eligible_fixture` vs 지시문 `fixture_unavailable_unverified`. 의미는 동일하나 다른 세션이 grep할 어휘가 다름 | 생성기(`_migrate_v1_to_v2.py` ORACLES)와 산출물(`oracle_manifest.json`) 양쪽 정합. **마이그레이션은 v1→v2 일회성이라 재실행 불가**(v2 fixture에 `evidence_items` 없어 KeyError) → 생성으로 고정할 수 없으므로 **드리프트 게이트 테스트 2건 신설** | **게이트** — `test_oracle_manifest_matches_the_migration_scripts_oracle_table`, `test_unobtained_fixture_uses_the_directive_status_vocabulary`. 한쪽만 되돌려 2건 실패 확인(양방향) |

### [DONE] #25 상세 — 제약 #11 리뷰 실행 기록 (2026-07-29)

**1. 교정이 인증의 선행 조건이었다.** 패턴 8("positive control이 없는 가드는
recall이 미상이고, 미상인 것을 안전 근거로 인용하면 안 된다")을 주석이 아니라
기계로 강제했다 — `_review_11.py`가 교정 기록 없이는 인증급 사용을 거부한다.

| 축 | 결과 |
|---|---|
| recall 5건(liveness/precedence로 판단한 rationale) | **5/5 `violation`**, 인용 전부 원문 일치 |
| precision 7건(계약이 *요구*하는 행동, 계약 문구에서 직접 추출) | **7/7 `ok`**, 오탐 0 |
| cardinality 3건(rationale 0/1/6개, evidence 0/1/3개) | **3/3** 정상 판정 |

**판별 케이스가 작동했다** — `커밋`·`stale`을 언급하되 서열을 매기지 않는 3건을
전부 `ok`로 통과시켰다. 리뷰어가 표면 어휘를 패턴매칭하는 게 아니라 "서열이
판단에 쓰였는가"를 판별한다는 뜻이다. 위반 문장이 `report` 끝에만 숨은
최난도 케이스도 그 문장을 정확히 인용해 지목했다.

**2. 단계적 조기중단(D5 정책)을 적용했다.** Stage A 9건(cell당 3) → 위반 0건으로
미달 조건(cell당 2건) 미도달 → Stage B 21건. 토큰 절감은 실패 조기 발견
경로에서만 실현되며, 성공 경로에서는 30건 전수가 필요하다는 점을 사전등록에
명시해 두었고 실제로 30건을 전부 리뷰했다.

**3. 실행 방식** — trial당 독립 호출(배치하면 앞 판정이 뒤로 샌다). 21건은 직접
호출, 마지막 9건은 dynamic workflow(loader 1 + reviewer 9). **양쪽 모두 schema를
전달하지 않았다** — 코호트 중간에 리뷰어 표면을 바꾸지 않기 위해서다.
워크플로 결과: 9/9, `agents_error` 0, `agents_empty_result` 0.

**4. 패턴 9 독립 재현** (`_verify_review_11.py`) — all-clean 보고는 legacy 유출
실행이 냈던 만장일치와 같은 모양이므로 그대로 받지 않았다. 리뷰어의 방법을
재사용하지 않고 **어휘 스캔**으로, `trials.json`이 아니라 **`trials_raw.json`**
에서 재현했다. 결과 **0/30 히트, 두 방법 일치.**

> **실패한 시도 기록**: 어휘 스캔 v1은 자기 측정에서 **recall 4/5**였다 —
> `recall-inside-a-long-report`의 `더 최근에`를 놓쳤다(정규식에 `최신`만 있었음).
> 미상인 도구의 침묵은 근거가 아니므로 어휘를 보강해 **v2에서 5/5**를 확보한
> 뒤에야 코호트에 적용했다. 이 경위 때문에 `self_check()`가 스크립트에
> 내장돼 있고, recall이 미달이면 **중단**한다.
>
> v2는 의도적으로 과민하다(계약 준수 케이스 2건 오탐). 과민한 도구의 침묵이라야
> 정보가치가 있기 때문이다.

**5. 정직하게 분리해 기록할 것**:

- ✅ **recall은 두 번째 독립 방법으로 재현됨** (다른 방법 + 더 원시적인 데이터)
- ❌ **cohort 수준 precision은 미검증** — 30 trial 중 알려진 위반이 없어
  negative control이 존재하지 않는다. 리뷰어 precision은 **교정 코퍼스에서만**
  측정됐다.
- 이 결과는 "#11이 검증됐다"는 뜻이지 **"#11 위반이 없음이 증명됐다"는 뜻이 아니다.**

**6. 부수 발견 — 뮤테이션 검증과 stale bytecode**: 검사기 가드를 일부러 깨뜨려
recall을 확인하던 중, 파일을 복원했는데도 실패가 계속 보고됐다. 원인은
`__pycache__` — 이 저장소의 실험 테스트는 전부 `spec_from_file_location`으로
모듈을 읽어서 변조된 소스의 캐시된 바이트코드를 집어온다. **뮤테이션 검증
사이에는 `rm -rf __pycache__`가 필요하다.** 그러지 않으면 존재하지 않는 코드에
대해 결과를 보고한다.

---

## 다음 세션 첫 행동

**2026-07-29 운영 지시 수신 상태. 신규 trial 실행은 차단 유지 중이다.**
지시문 8개 차단 조건은 전부 충족됐고(§0 표), 남은 것은 **설계 담당 판정 2건**이다.

1. **D4 (최우선, 지시문이 잡아낸 실질 결함)** — 제약 #11이 `UNKNOWN`인 채로
   3/3 인증이 났다. 지시문 §3은 그 경우 실행 차단을 요구한다. (a)/(b)/(c) 중
   선택 필요, **(a) 권장**(기존 30 trial rationale 재검토, 새 trial 불필요).
   승인되면 `_review_11.py` 신설 → 결과를 `clean` 정의 4번째 항으로 편입.
2. **D5** — 재실행 규모 17(지시문 문자) vs 30(사전등록 N=10/cell) 충돌 판정.
   30 유지면 조치 없음. 17 고수면 재동결·재실행 + 현 30 trial을
   `non_certifying_partial`로 강등.
3. **D3(H1c)** — H3 커버리지 재설계. 결정 없이는 H3 스코핑 불가.
4. D3 결정 후 **`_gen_prompts.py`** 스코핑 → H3 실행.
5. 병행 가능, D3·D4와 무관: **H1a** fixture 제작(재료 확보됨,
   `docs/phase_a_implementation_packet.md:102` 대 R6b/`cg_partwhole.py`).

**D4·D5 판정 전에는 신규 trial을 실행하지 않는다**(지시문 §2 준수).
